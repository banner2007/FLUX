{
  "name": "flux-app-root",
  "version": "1.0.0",
  "description": "Root package to handle Railway deployment",
  "scripts": {
    "postinstall": "cd backend && npm install",
    "start": "cd backend && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {}
}